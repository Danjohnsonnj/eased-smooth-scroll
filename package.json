{
  "name": "untitled",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "publish": "npm run clean && npm run build && git subtree push --prefix dist origin gh-pages",
    "clean": "rm -rf dist && rm -rf .cache",
    "dev": "parcel *.html --https",
    "build": "parcel build --public-url . *.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "less": "^3.12.2",
    "parcel-bundler": "^1.12.4"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
